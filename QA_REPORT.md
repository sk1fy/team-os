# Frontend QA report — TeamOS

> **Remediation:** дефекты отчёта исправлены на frontend в ветке `codex/qa-audit-fixes` и локально перепроверены. Валидация телефона реального HTTP-backend остаётся отдельной задачей backend-команды; в этом репозитории добавлена frontend- и mock-API-защита. Актуальная таблица статусов находится в [QA_AUDIT.md](QA_AUDIT.md).

Дата прогона: 17.07.2026<br>
Стенд: `http://31.76.42.128:8080`<br>
Среда: Codex in-app Chromium, desktop 1280×800 и mobile 375×812, светлая тема<br>
Аккаунт: владелец `6001517@gmail.com`

## Сводка

Это первый приоритетный прогон P0 → P1. Найдено 9 дефектов: 5 × SEV-2, 4 × SEV-3. Крашов, XSS и ошибок в консоли в пройденных сценариях не обнаружено.

| Раздел плана | Статус | Результат |
|---|---|---|
| 1. Неправильный ввод | failed | Телефон принимает буквы; email `a@b` проходит клиентскую проверку; пробелы в имени сотрудника корректно отклоняются, крайние пробелы обрезаются; поиск устойчив к `(`, `[`, `*`, `\\` и XSS-строке |
| 2. Загрузка / ошибки / пустота | failed | Бесконечные skeleton/пустой экран для части неизвестных ID; пустой kanban без EmptyState и действия; принудительные ошибки API skipped — опубликованный стенд не позволяет менять `ERROR_RATE` |
| 3. Адаптивность | passed (smoke) | 375×812: body без горизонтального скролла; календарь и модалка сотрудника помещаются, фон модалки заблокирован; полный покомпонентный прогон ещё не завершён |
| 4. Кроссбраузер | skipped | Firefox/Safari/Android Chrome недоступны в текущем окружении |
| 5. Доступность | failed | После `Esc` фокус не возвращается на триггер группы распределения; сообщения валидации статьи отсутствуют; полный аудит контраста ещё не завершён |
| 6. Валидация и submit | failed | Пустая статья молча не сохраняется; формы входа и сотрудника используют native validation без inline-error; успешное создание сотрудника обновляет список без F5 |
| 7. Повторные действия | skipped | Деструктивный double-click прогон отложен до отдельного тестового набора данных |
| 8. Навигация / URL / F5 | failed | Редиректы `/auth`, `/auth/create-company`, `/structure` работают; обычный 404 работает; title после login остаётся от страницы входа |
| 9. Роли и права | partial pass | Владелец определён корректно; полноценная employee-role проверка не запускалась на опубликованном стенде |
| 10. Производительность | partial pass | Консоль чистая; build предупреждает о JS chunk 1.35 MB; нагрузочный набор не запускался |
| 11. CRUD | failed | Сотрудник: create/list/delete и trim прошли; невалидный телефон сохраняется. Остальные модули ещё не пройдены полностью; задачи блокируются пустой доской без создания колонки |

## Дефекты

### [SEV-2] Телефон сотрудника принимает и сохраняет произвольные буквы

- **URL/route:** `/employees` (модалка «Добавить пользователя» → drawer сотрудника)
- **Окружение:** Chromium, viewport 1280×800, светлая тема, опубликованный стенд
- **Шаги:**
  1. Открыть «Добавить пользователя».
  2. Заполнить обязательные поля валидными данными.
  3. В поле «Телефон» ввести `abcdef`.
  4. Нажать «Добавить» и открыть созданного сотрудника.
- **Ожидаемое:** буквы отклоняются, у поля показано русское сообщение; сотрудник не создаётся до исправления телефона.
- **Фактическое:** форма успешно создаёт сотрудника; drawer показывает сохранённый телефон `abcdef`.
- **Скрин:** [invalid_phone_employee.png](qa-screenshots/invalid_phone_employee.png)
- **Консоль:** чисто
- **Сеть:** не инспектировалась доступным браузерным интерфейсом
- **Воспроизводимость:** стабильно, 1 из 1

### [SEV-2] Несуществующий сотрудник оставляет бесконечный skeleton без 404

- **URL/route:** `/employees/nonexistent-id`
- **Окружение:** Chromium, viewport 1280×800, светлая тема, опубликованный стенд
- **Шаги:**
  1. Открыть URL напрямую.
  2. Подождать не менее 3.5 секунд.
- **Ожидаемое:** страница «Сотрудник не найден» с возможностью вернуться к списку.
- **Фактическое:** AppLayout отображается, рабочая область остаётся в skeleton-состоянии без текста ошибки и без кнопки.
- **Скрин:** [employees_nonexistent-id.png](qa-screenshots/employees_nonexistent-id.png)
- **Консоль:** чисто
- **Сеть:** не инспектировалась доступным браузерным интерфейсом
- **Воспроизводимость:** стабильно, 2 из 2 переходов

### [SEV-2] Несуществующая группа распределения оставляет пустую рабочую область

- **URL/route:** `/distribution/fake`
- **Окружение:** Chromium, viewport 1280×800, светлая тема, опубликованный стенд
- **Шаги:**
  1. Открыть URL напрямую.
  2. Подождать не менее 3.5 секунд.
- **Ожидаемое:** русское состояние «Группа не найдена» с возвратом к списку групп.
- **Фактическое:** виден только AppLayout; контент, ошибка и действие возврата отсутствуют.
- **Скрин:** [distribution_fake.png](qa-screenshots/distribution_fake.png)
- **Консоль:** чисто
- **Сеть:** не инспектировалась доступным браузерным интерфейсом
- **Воспроизводимость:** стабильно, 2 из 2 переходов

### [SEV-2] Несуществующий курс показывает только заголовок «Курс»

- **URL/route:** `/learn/fake`
- **Окружение:** Chromium, viewport 1280×800, светлая тема, опубликованный стенд
- **Шаги:**
  1. Открыть URL напрямую.
  2. Подождать не менее 3.5 секунд.
- **Ожидаемое:** русское состояние «Курс не найден» и действие выхода в Академию.
- **Фактическое:** полноэкранная страница содержит только заголовок «Курс»; объяснения и выхода нет.
- **Скрин:** [learn_fake.png](qa-screenshots/learn_fake.png)
- **Консоль:** чисто
- **Сеть:** не инспектировалась доступным браузерным интерфейсом
- **Воспроизводимость:** стабильно, 2 из 2 переходов

### [SEV-2] Пустая доска задач не имеет EmptyState и не позволяет создать колонку/задачу

- **URL/route:** `/tasks`, вкладка «Канбан»
- **Окружение:** Chromium, viewport 1280×800, светлая тема, опубликованный стенд
- **Шаги:**
  1. Открыть `/tasks` в компании без задач/колонок.
  2. Дождаться завершения загрузки.
- **Ожидаемое:** осмысленный EmptyState и кнопка создания первой колонки или задачи.
- **Фактическое:** под вкладками остаётся пустая область; кнопок создания нет, начать CRUD-цикл невозможно.
- **Скрин:** [tasks_empty_blank.png](qa-screenshots/tasks_empty_blank.png)
- **Консоль:** чисто
- **Сеть:** не инспектировалась доступным браузерным интерфейсом
- **Воспроизводимость:** стабильно, 2 из 2 переходов

### [SEV-3] Пустая/пробельная статья не показывает причину отказа в сохранении

- **URL/route:** `/knowledge` (модалка «Новая статья»)
- **Окружение:** Chromium, viewport 1280×800, светлая тема, опубликованный стенд
- **Шаги:**
  1. Нажать «Статья».
  2. Оставить форму пустой и нажать «Сохранить».
  3. Повторить с названием из трёх пробелов.
- **Ожидаемое:** ошибки у обязательных полей, фокус на первой ошибке; пустая статья не создаётся.
- **Фактическое:** сущность не создаётся, но визуальной ошибки, toast, `aria-invalid` и переноса фокуса нет; фокус остаётся на кнопке «Сохранить».
- **Скрин:** не требуется: дефект поведения подтверждён DOM-состоянием
- **Консоль:** чисто
- **Сеть:** запрос не наблюдался
- **Воспроизводимость:** стабильно, 2 из 2 вариантов

### [SEV-3] Email `a@b` считается валидным на форме входа

- **URL/route:** `/auth/login`
- **Окружение:** Chromium, viewport 1280×800, светлая тема, опубликованный стенд
- **Шаги:**
  1. Ввести `a@b` в Email.
  2. Перевести фокус или попытаться отправить форму.
- **Ожидаемое:** адрес без доменной зоны отклонён с русским сообщением.
- **Фактическое:** `input.validity.valid === true`, клиентская ошибка отсутствует.
- **Скрин:** не требуется: дефект валидации
- **Консоль:** чисто
- **Сеть:** запрос с этим значением не отправлялся
- **Воспроизводимость:** стабильно, 1 из 1

### [SEV-3] После успешного входа остаётся title страницы входа

- **URL/route:** `/auth/login` → `/`
- **Окружение:** Chromium, viewport 1280×800, светлая тема, опубликованный стенд
- **Шаги:**
  1. Войти с валидными тестовыми данными.
  2. Дождаться дашборда.
  3. Проверить `document.title`.
- **Ожидаемое:** title дашборда, например `TeamOS` или `Главная — TeamOS`.
- **Фактическое:** URL и контент уже `/`, но title остаётся `Вход — TeamOS` более 1.7 секунды. Дополнительно `/notifications` и 404 используют общий `TeamOS` вместо уникального title.
- **Скрин:** не требуется: метаданные страницы
- **Консоль:** чисто
- **Сеть:** не релевантно
- **Воспроизводимость:** стабильно, 1 из 1

### [SEV-3] После закрытия модалки группы по Esc фокус не возвращается на триггер

- **URL/route:** `/distribution` (модалка «Новая группа распределения»)
- **Окружение:** Chromium, viewport 1280×800, светлая тема, опубликованный стенд
- **Шаги:**
  1. Нажать «Создать группу».
  2. Нажать `Esc` внутри поля названия.
  3. Проверить активный элемент.
- **Ожидаемое:** модалка закрывается, фокус возвращается на кнопку «Создать группу».
- **Фактическое:** модалка закрывается, но активным становится `body`; клавиатурный пользователь теряет позицию.
- **Скрин:** не требуется: дефект фокуса
- **Консоль:** чисто
- **Сеть:** не релевантно
- **Воспроизводимость:** стабильно, 1 из 1

## Успешно проверено

- Валидный вход ведёт на дашборд.
- `/auth` → `/auth/login`, `/auth/create-company` → `/auth/register`, `/structure` → `/employees`.
- Обычный неизвестный URL показывает полноценную 404 со ссылкой «На главную».
- `/share/article/fake` и невалидный invite-token показывают осмысленные русские состояния ошибки.
- Создание сотрудника обновляет список без F5; крайние пробелы имени и фамилии обрезаются.
- Имя из одних пробелов отклоняется, введённые данные остаются в модалке.
- Поиск сотрудников не падает на `(`, `[`, `*`, `\\`, `<script>alert(1)</script>`; отображается отдельное состояние «Никого не нашлось».
- Мобильная модалка сотрудника помещается в 375×812, фон блокируется (`body overflow: hidden`), `role="dialog"` и `aria-labelledby` присутствуют.
- Mobile smoke на основных маршрутах не выявил горизонтального скролла body.
- Browser console: ошибок и warning-ов во время пройденной навигации нет.
- Автотесты: 11 файлов / 67 тестов passed.
- Build: passed.
- Lint: passed с 5 warning по зависимостям React hooks.

## Временные данные и изменения

- Созданы два тестовых сотрудника: trim-сценарий и сценарий телефона `abcdef`.
- Оба сотрудника удалены; счётчик вернулся с 8 к исходным 7, toast «Сотрудник удалён» получен.
- `ERROR_RATE`, `CURRENT_USER_ID`, fixtures и исходный код не изменялись.
- В рабочее дерево добавлены только этот отчёт и QA-скриншоты; перед началом `QA_PLAN.md` уже был untracked.

## Автоматические проверки

```text
npm test      — passed: 11 files, 67 tests
npm run build — passed; warning: bundle index JS 1,346.87 kB (gzip 399.33 kB)
npm run lint  — passed with 5 warnings:
  EmployeeDrawer.tsx — 3 react-hooks/exhaustive-deps
  SchedulePage.tsx   — 2 react-hooks/exhaustive-deps
```
